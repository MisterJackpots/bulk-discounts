<h1>Create a New Discount</h1>

<%= render partial: 'form', locals: {
                                      model: [:merchant, @discount],
                                      button_text: 'Create Discount'} %>
